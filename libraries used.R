library(plumber)   # Used to create REST APIs in R, converting R functions into web-accessible endpoints.  
library(tesseract) # Used for Optical Character Recognition (OCR) to extract text from images.  
library(httr)      # Used to send HTTP requests (GET, POST, etc.) for interacting with web APIs.  
library(jsonlite)  # Used for reading, writing, and processing JSON data in R.  
